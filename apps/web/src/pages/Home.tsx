import { Grid } from "@mui/material";
import BlogCard from "../components/BlogCard";

const Home = () => {
  return (
    <div>
      <Grid>{/* <BlogCard  />     */}</Grid>
    </div>
  );
};

export default Home;
